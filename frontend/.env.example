# 前端环境变量示例
# 复制为 .env.local / .env.development 覆盖使用

VITE_APP_TITLE=MaiMai Launcher

# ================= API 基地址策略 =================
# 1. 若直接本地运行后端源码 (python) 推荐:
# VITE_API_BASE_URL=http://localhost:7999/api
# 2. 若通过 docker-compose 运行并使用容器内访问服务名，可在构建时传入 build arg
# (Dockerfile 已支持 ARG API_BASE) 一般不需要改这里。
# 3. 若使用反向代理 (前端与后端同域 /api 转发)，可设:
# VITE_API_BASE_URL=/api
VITE_API_BASE_URL=/api

# WebSocket 基地址 (如后端未来提供 ws)
VITE_WS_BASE_URL=ws://localhost:8080/ws

VITE_NODE_ENV=development
